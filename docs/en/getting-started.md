<!-- tiger:doc
title: Getting started
order: 20
visibility: public
-->

# Getting started

*(Placeholder.)* Install into `application/modules/theme-grey-mist/`, then **Activate** it in the
Module Manager (Themes). It's one page (`content/index.phtml`) — add more by dropping
`content/<slug>.phtml` files with a `tiger:page` hint.
